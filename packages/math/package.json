{
  "name": "@nebula/math",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "build": "tsc -p tsconfig.json --noEmit",
    "type-check": "tsc -p tsconfig.json --noEmit",
    "test": "vitest --run",
    "clean": "pwsh -NoProfile -Command \"if (Test-Path dist) { Remove-Item -Recurse -Force dist }\""
  }
}
